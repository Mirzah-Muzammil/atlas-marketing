# Atlas Marketing

Production marketing site for Atlas, built with Next.js 15, React 19, and Tailwind CSS 4.

## Development

```bash
npm install
npm run dev
```

The homepage is served from `/`.

## Quality checks

```bash
npm test
npm run typecheck
npm run lint
npm run build
```

The archived multi-route site lives separately at `../atlas-old-landing`.
