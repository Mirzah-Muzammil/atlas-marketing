import type { ReactNode } from "react";

import "./landing-2.css";

export default function Landing2Layout({ children }: { children: ReactNode }) {
  return <div className="landing-2-route">{children}</div>;
}
